{
  "name": "@ai-engine/planner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Multi-step execution planner for agents — generates and executes tool-based plans",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ai-engine/core": "workspace:*"
  }
}
