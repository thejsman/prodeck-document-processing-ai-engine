{
  "name": "@ai-engine/types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared type contracts for the AI document processing engine",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  }
}
