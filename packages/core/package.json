{
  "name": "@ai-engine/core",
  "version": "0.1.0",
  "private": true,
  "description": "Core pipeline execution, validation, and registry logic",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ai-engine/types": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^4.0.18"
  }
}
